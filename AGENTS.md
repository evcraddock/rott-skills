# Development Guidelines

## Skills

Each skill is a directory under `skills/` containing a `SKILL.md` file with:
- YAML frontmatter (name, description, allowed-tools)
- Examples of user prompts and corresponding CLI commands
- Notes on usage
